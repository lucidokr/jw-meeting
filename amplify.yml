version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run fe:build
  artifacts:
    baseDirectory: modules/
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
